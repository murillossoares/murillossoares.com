# Component Specification

## States
idle, hover, active, disabled, loading, error

## Accessibility
ARIA roles, keyboard navigation, screen reader labels, prefers-reduced-motion

## Theme
Must work in all 3 themes using tokens only

## Responsive
Mobile-first, 320px+ support, touch targets >= 44px

## i18n
All strings externalized
